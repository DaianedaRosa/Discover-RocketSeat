// Object

const person = {
    name: 'Daiane',
    age: 18,
    weight: 72.4,
    isAdmin: true
  }
  // Para acessar propriedades de um Objeto usamos o ".";
  console.log(`${person.name} tem ${person.age} anos`)
